[package]
name = "iron-vaak-ffi"
version = "0.0.0"
edition = "2021"
license = "MIT"
publish = false
description = "Safe native ABI core scaffold for IRON VAAK"

[lib]
name = "iron_vaak_ffi"
crate-type = ["rlib", "cdylib", "staticlib"]

[dependencies]
vaak = { path = "../.." }

