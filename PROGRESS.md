# 進捗

**この文書は、文脈が圧縮されても失われないための記録である。**
**段階が終わるたびに更新する。憶測を書かない——動いたものだけを「済」にする。**

最終更新：S-5（バイトコード VM）完了

---

## 現在地

**speculative 枝。** 未定部分を私が決めて（S-0〜S-4）実装した。
**P0〜P9 完了。** 木を辿る実装とバイトコード VM が同じ結果を出す。

テストは合計 189 件（差分テストは 100 例以上を両実装に掛ける）。`cargo test` で全部通る。

**決定 C-1 〜 C-92 と実装の照合が済んでいる**（`tests/decisions.rs` 47 件、C-93 に記録）。

## 段階の状態

| | 段階 | 状態 | 備考 |
|---|---|---|---|
| P0 | 足場 | **済** | Cargo。最適化はしない（動くものが先） |
| P1 | 字句解析 | **済** | `src/lexer.rs`。`tests/lex.rs` 17 件 |
| P2 | AST とパーサ | **済** | `src/ast.rs` `src/parser.rs`。`tests/parse.rs` 18 件 |
| P3 | 値・セル・アリーナ | **済** | `src/value.rs`。領域ごとのアリーナ（mark/release） |
| P4 | 評価器 | **済** | `src/interp.rs`。`tests/semantics.rs` 48 件 |
| P5 | 静的検査器 | **済** | `src/check.rs`。`tests/check.rs` 19 件 |
| P5b | 型検査 | **済** | `src/types.rs`。`tests/types.rs` 16 件 |
| P6 | 標準ライブラリ | **済** | 配列 6 / 写像 4 / `utf8_*` 3。境界は S-3 |
| P7 | ホスト界面 | **済** | `src/host.rs`。`tests/host.rs` 7 件。契約は S-4 |
| P8 | 意味論テスト | **済** | プローブの全項目が `tests/` にある（lex 17 / parse 18 / semantics 48 / check 17）|
| P9 | バイトコード VM | **済** | `src/vm.rs`。`tests/differential.rs` で木と照合 |

## VM で気をつけること（次に触る人へ）

- **意味論を二度実装しない。** 値・算術・標準ライブラリ・経路は `interp.rs` を呼ぶ。
  VM が持つのは「どう辿るか」だけ
- **段は明示的に持つ**（`StageState`）。木を辿る実装では再帰の深さが段だが、VM では持たねばならない。
  何が段かは C-64 の表そのまま
- **`outward` はビット列**。段を送るたびに最下位ビットを見てシフトする（C-70）
- **差分テストが唯一の検証手段。** `tests/differential.rs` に例を足すこと。
  VM を直したら必ず走らせる

## 分かっている問題

（無し。既知の妥協は P5b で解消し、決定との食い違いは C-93 で解消した）

## 照合で見つかったもの（C-93）

- **`new P ( )`** が評価器で落ちていた（型検査器は通していた）。**層の間で扱いが割れていた**
- **`continue` の被演算子の名前解決**が本体の先頭で行われていなかった（C-81 違反）。
  `visible_limit` を入れて直した

## P4 で分かった実装上の注意（次に触る人へ）

- **宣言は `;` に包まれる。** `fn f () {} ;` は `Discard(FnDecl)` になるので、
  宣言の収集は `Discard` を剥がしてから見る。ここで一度落ちた
- **`outward` は厳密。** `EscapeVal.outward` は**ビット列**で、
  「何回目の段送りが越えるか」を持つ（C-70 どおり）。
  実行時も検査器も位置で見る。**空振りの `outward` は静的エラー**
- **`loop` の反復回数**は `i64` で折り返す。無限ループはテストに書けないので、
  `break` で抜ける形でしか確かめていない
- **型検査は `src/types.rs` にある。`check.rs` とは別の層。**
  `check.rs` = 領域の値の数・段数・権限・別名・宣言の位置（**効果と能力**）。
  `types.rs` = 型だけ。混ぜると両方読めなくなる
- 型検査は**双方向**。リテラルだけが置かれた場所の型を受け取り、無ければ既定（i64 / f64）。
  分からない型（`None`）では**何も言わない**——誤検出を出さないため
- 脱出が運ぶ値の型は `stage_want` を段ごとに積んで照合する。
  段数が静的に決まらない場合（`$repeat` 等）は照合しない

## P5 で分かったこと

- **段の種類を積まないと `continue` の行き先が分からない。** 「直近の段がループか」だけでは
  `loop { { continue; }; }` を捕まえられない。`Vec<Stage>` にした
- 検査器は**型検査器ではない**——名前解決・領域の値の数・段数・権限・別名・宣言の位置。
  型そのものはまだ検査していない（リテラルの型解決だけ）

## 実装で見つかった設計の穴（decisions.md に記録済み）

- **C-91 (1)** 写像リテラルの先読みが `switch` の腕の `=>` を誤認していた。`case` を数えて直した
- **C-91 (2)** 「文法エラー」と書いていた二例（`&= a[0]` / `continue 1`）は、
  実際には別の木として解析が通る。**解析器がその位置で止めて意図を言う**ようにした
- **C-92** `continue continue` は「二段進める」のではなく「周回を一つ余分に飛ばす」。
  プローブの注釈が誤っていた

## 後回しにしたもの

- **Zed のシンタックスハイライタ**——言語が動いてから。他のエディタは要らない

## 未決定のまま実装に入ったもの

- **標準ライブラリの境界**——`$return` `.len()` `.push()` `$repeat` `getdepth()` だけ実装する
- **ホスト界面の契約**——最小限（`&=` でスライスに張れるだけ）
- **利用者がメンバ関数を宣言する構文**——実装しない。組み込みのみ
- **ラップ型を宣言する構文**——実装しない。`str` のみ組み込み
- **別名の判定**は名前の比較のみ（C-89 の段階 1）

## 決めたが実装で確かめていないこと

（P4 以降で埋める）
